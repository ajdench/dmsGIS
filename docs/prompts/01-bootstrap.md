# Prompt: Bootstrap the repository

Read `README.md`, `AGENTS.md`, and `docs/specification.md` first.

Bootstrap this repo as a React + TypeScript + Vite app using OpenLayers, Zustand, Zod, Vitest, and Playwright.

Requirements:
- Create the structure described in the docs
- Add a desktop-first shell with top bar, left sidebar, map panel, right sidebar
- Add placeholder feature modules
- Add small sample fixture data and manifests under `public/data/`
- Ensure the app runs locally

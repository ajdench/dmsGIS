# Prompt: Implement map core and layer loading

Read `README.md`, `AGENTS.md`, and `docs/specification.md` first.

Requirements:
- Create the OpenLayers map module
- Load a base UK-focused view
- Load a layer manifest from `public/data/manifests/layers.manifest.json`
- Render one polygon layer and one point layer
- Add a layer panel with visibility and opacity controls
- Use TypeScript types and Zod validation

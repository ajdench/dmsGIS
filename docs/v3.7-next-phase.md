# v3.7 Next Phase

This note is now historical context for the rejected `v3.7` compare work.

The approved replacement direction for the next execution phase is now:

- `docs/v3.8-bsc-runtime-family-spec.md`

This note marks the next version boundary after the locked `v3.6` baseline.

`v3.7` is now in progress, and its first shipped slice is live for review.

It should begin from:

- `docs/current-app-baseline-v3.6.md`

## Intent

Treat `v3.7` as the next execution phase after rebuild-formalization is complete.

It should preserve the locked `v3.6` app contract while addressing only genuinely remaining geometry/runtime issues, not reopening already-resolved `v3.5` and `v3.6` seams.

## Red Lines

- do not weaken or bypass `scripts/validate-runtime-geometry-family.mjs`
- do not change sidebar/store/control behavior casually
- do not reopen split-ICB, mask, or outline work unless a current defect is demonstrated against the locked `v3.6` baseline
- do not treat historical `v3.4`/`v3.5` experimentation as the active contract

## Start Condition

Before any `v3.7` work:

1. read `docs/current-app-baseline-v3.6.md`
2. read `docs/agent-handover.md`
3. confirm the issue exists on the live `v3.6` baseline
4. preserve the current product-family validation gate

## Current Start State

The first `v3.7` slice is the staged water-edge formalization pass:

- `docs/v3.7-water-edge-staged-approach.md`

That slice defines shared preprocessing tolerances and runtime visual tokens before any hydro/coastal geometry behavior is changed.

The current completed sub-slice inside that work is:

- hydro-normalized landmask profile generation for review
- `standard` profile activated on the rebuild path for review cutover
- active clipping now uses only `inlandWater` deltas from that hydro-normalized profile, not all classified deltas
- review-time classified water-edge arc helper products for both `Current` and `2026`
- not yet an accepted final baseline

The next approved branch of `v3.7` work was documented in:

- `docs/v3.7-coastal-envelope-compare-plan.md`

That compare plan supersedes the idea that runtime water-edge masking is the final solution.
The new direction is to build two preprocessing-driven external-envelope compare families:

- a `bfe` product
- a `bsc` product

That source-acquisition branch is still useful as historical/input context, but it is no longer the preferred execution model.

The initial source-acquisition start for that branch is complete under:

- `geopackages/compare_sources/`
- `geopackages/compare_sources/v37_coastal_compare_sources.json`

Current local compare-source bundle:

- England ICB `BFC`: `42` features
- England ICB `BGC`: `42` features
- Wales LHB `BFC`: `7` features
- Wales LHB `BGC`: `7` features

The compare result is now considered insufficient.

The new next-step execution brief is:

1. revert to a stable full-UK baseline state
2. rebuild the runtime families directly from `BSC-first` board sources
3. preserve the existing app contract
4. keep the land-sea mask workflow
5. rebuild `Current` split-ICB runtime against the new `BSC` parents

Use:

- `docs/v3.8-bsc-runtime-family-spec.md`

# v3.3 Sea Patch Reactivation Note

This note records the seam that existed before the stronger `v3.3` sea-patch finish, and the route that was used to complete it.

## Current Stable Baseline

- Repo: `/Users/andrew/Projects/dmsGIS`
- Live app: `http://127.0.0.1:4173/dmsGIS/`
- Current recoverable baseline: `docs/current-app-baseline-v3.3.md`
- JJ checkpoint for that baseline: `b8537df3` — `Establish v3.3 UK basemap coast alignment baseline`

The repo is no longer paused on that seam. Keep this note only as historical context if the UK sea patch needs to be revisited later.

## What Is Already True

- `v3.2` already moved the app boundary/coast products onto the OSM-derived coastline truth during preprocessing.
- The current shipped `v3.3` improved the visible UK coast by:
  - using a filtered UK/Ireland visible land asset at `public/data/basemaps/uk_landmask_osm_v01.geojson`
  - removing the stray continental polygon that had created the visible box artifact
  - tying the visible UK land alignment to the existing global `Land` and `Sea` controls
- The repo is green at the `v3.3` baseline:
  - `npm run test -- --run`
  - `npm run test:e2e`
  - `npm run build`

## Resolved Seam

The seam that remained after the first shipped `v3.3` attempt was:

- visible UK `Land` already used the filtered OSM-derived UK/Ireland land shape
- visible UK `Sea` was still only a halo approximation rather than a true matched companion patch

That seam is now resolved in the stronger `v3.3` finish:

1. visible UK `Land` uses the filtered OSM-derived UK/Ireland land shape
2. visible UK `Sea` uses the local complement of that same shape in `public/data/basemaps/uk_seapatch_osm_v01.geojson`
3. app product coasts remain on the same `v3.2` coastline truth underneath

## What Was Attempted Just Before Pause

The abandoned direction was:

- derive a local UK sea patch from a neighborhood box
- subtract the filtered UK/Ireland OSM land shape
- subtract nearby non-UK land blockers so the sea patch would not paint over France / the continent

The work stopped before any further repo changes were applied. Only temporary `/tmp` files were used during this attempt.

Important: do not resume from the discarded temporary coastal-mask approach. The earlier visible box came from:

- `public/data/basemaps/uk_basemap_alignment_mask_v01.geojson`
- `public/data/basemaps/uk_coastal_alignment_mask_v01.geojson`

Those failed experiments have already been removed from the repo and should stay removed.

## Useful Known Inputs

- Visible UK land asset:
  - `public/data/basemaps/uk_landmask_osm_v01.geojson`
- Wider visible basemap sources:
  - `public/data/basemaps/ne_10m_land.geojson`
  - `public/data/basemaps/ne_10m_ocean.geojson`
  - `public/data/basemaps/ne_10m_admin_0_countries.geojson`
- Preprocessing coastline source family:
  - `geopackages/coastline_sources/simplified-land-polygons-complete-3857.zip`
- Current runtime wiring:
  - `src/features/map/MapWorkspace.tsx`
  - `src/features/map/mapWorkspaceLifecycle.ts`

## Recommended Resume Point

Resume by building a real UK-local visible sea patch, not another mask/halo approximation.

Preferred route:

1. derive a UK-local sea polygon from the same coastline source family as `uk_landmask_osm_v01.geojson`
2. keep the wider world basemap untouched
3. wire that UK sea patch to the existing global `Sea` control
4. keep the UK land patch wired to the existing global `Land` control
5. preserve the single-map runtime architecture

## Guardrails

- Do not introduce a new sidebar row or control for the UK alignment layers.
- Keep UK visible `Land` and UK visible `Sea` tied to the existing global Land/Sea controls.
- Do not regress the already-clean `v3.3` no-box state.
- Do not move this work into `v3.4`; `v3.4` is still the river/inland-water cleanup pass.
